<% unless @user == current_user %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'shared/unfollow_user' %>
  <% else %>
    <%= render 'shared/follow_user' %>
  <% end %>
  </div>
<% end %>