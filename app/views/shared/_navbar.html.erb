<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand">
        <% if user_signed_in? %>
          <%= link_to "Hannibal", current_user %>
        <% else %>
          <%= link_to "Hannibal", root_path %>
        <% end %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li class="active"><%= link_to "logout", destroy_user_session_path, :method => :delete %></li>
          <li>Hello <%= current_user.email %></li>
        <% end %>
          <li class=""><%= link_to "login", new_user_session_path %></li>
          <li class=""><%= link_to "Register", new_user_registration_path %></li>
          <li class=""><%= link_to "Index", root_path %></li>
          <!--  -->
        
      </ul>
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>